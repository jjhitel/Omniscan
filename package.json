{
    "name": "omniscan",
    "version": "1.0.2",
    "description": "Search various blockchain explorers from the address bar.",
    "type": "module",
    "scripts": {
        "test": "node scripts/validate-engines.js",
        "dev:chrome": "cross-env TARGET=chrome vite",
        "dev:firefox": "cross-env TARGET=firefox vite",
        "build:chrome": "cross-env TARGET=chrome node scripts/prebuild.js && cross-env TARGET=chrome vite build && cross-env TARGET=chrome node scripts/package.js",
        "build:firefox": "cross-env TARGET=firefox node scripts/prebuild.js && cross-env TARGET=firefox vite build && cross-env TARGET=firefox node scripts/package.js"
    },
    "author": "jjhitel",
    "license": "GPL-3.0-or-later",
    "devDependencies": {
        "archiver": "^7.0.1",
        "cross-env": "^10.0.0",
        "vite": "^7.1.4",
        "vite-plugin-web-extension": "^4.4.5"
    }
}
